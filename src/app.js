import React from "react"
import ReactDOM from "react-dom"
import RootContainer, {Root} from "./root"

ReactDOM.render(<Root/>, document.getElementById("app"));
//ReactDOM.render(<div>{"hi2"}</div>, document.getElementById("app"));